pipeline {
    agent any
    stages {
        stage('Build docker image') {
            steps {
                sh 'docker build -t kakachisensei/astonvilla-front:1.0.${env.BUILD_NUMBER} .'
                sh 'docker images'
            }
        }
    }
}
