pipeline {
    agent any
    stages {
        stage('Clean workspace') {
            steps {
                sh 'sudo rm -rf /root/.jenkins/workspace/astonvilla/*'
                sh 'ls'
            }
        }
        stage('Clean workspace') {
            steps {
                git clone "https://github.com/Raouf-Rhimi/astonvilla-app.git"
                sh 'ls'
            }
        }
    }
}
